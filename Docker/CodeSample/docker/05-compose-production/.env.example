# Production should use an immutable, approved digest.
APP_IMAGE=registry.example.com/team/api@sha256:REPLACE_ME
DB_IMAGE=postgres:17-alpine
PROXY_IMAGE=nginxinc/nginx-unprivileged:alpine
BIND_ADDRESS=0.0.0.0
PUBLIC_PORT=8080
