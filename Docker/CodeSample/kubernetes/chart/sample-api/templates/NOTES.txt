sample-api has been installed.

Wait for readiness:
  kubectl rollout status deployment/{{ include "sample-api.fullname" . }} -n {{ .Release.Namespace }} --timeout=3m

Open a local tunnel:
  kubectl port-forward service/{{ include "sample-api.fullname" . }} -n {{ .Release.Namespace }} 8080:{{ .Values.service.port }}

Then call:
  curl http://localhost:8080/
